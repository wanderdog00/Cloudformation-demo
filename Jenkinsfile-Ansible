pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
                sh "aws cloudformation update-stack --stack-name s3bucket --template-body file://ansible_training.yml --region 'eu-west-1'"
            }
        }
    }
}